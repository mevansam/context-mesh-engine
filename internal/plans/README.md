# plans

Internal Arazzo catalog, shared runner (`Run` / `Query`), MCP `query` + `run_*` registration, and OpenAPI generation.

**Not** part of the public SDK. Applications import `engine` and `arazzo` only.

Contributor contract: [docs/contributors/arazzo.md](../../docs/contributors/arazzo.md).
